/* SKILLAPP — application bootstrap.
   The codex, the atelier, the ledger, the rite. Filled in subsequent commits. */
(function () {
  "use strict";
  const view = document.getElementById("view");
  if (!view) return;
  view.innerHTML =
    '<section class="placeholder"><p>The codex is being typeset.</p></section>';
})();
